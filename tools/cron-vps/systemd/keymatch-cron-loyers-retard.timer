[Unit]
Description=KeyMatch cron timer loyers-retard — schedule "0 8 * * *"

[Timer]
OnCalendar=*-*-* 08:00:00
Persistent=true
RandomizedDelaySec=2min
AccuracySec=1min

[Install]
WantedBy=timers.target
